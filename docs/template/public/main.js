// The two-state brand logo is done entirely in main.css (see .navbar-brand rules),
// so no navbar JavaScript is needed. This empty default export keeps the DocFX
// modern template's dynamic import of main.js happy.
export default {};
