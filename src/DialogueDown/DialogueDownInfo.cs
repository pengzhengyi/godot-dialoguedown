namespace DialogueDown;

/// <summary>
/// Placeholder anchor type for the DialogueDown assembly.
/// Replace with the real dialogue graph / runner types as the library grows
/// (e.g. DialogueNode, DialogueChoice, IDialogueRunner, effects and conditions).
/// </summary>
public static class DialogueDownInfo
{
    /// <summary>The library's stable identifier.</summary>
    public const string Name = "DialogueDown";
}
