node_modules/
coverage/
playwright-report/
test-results/
.vite/

# The single-file build output is committed for the .NET embed (freshness is
# verified in CI); ignore everything else Vite may emit under dist/.
dist/*
!dist/report.html

# Live e2e temp document
e2e-live/.live-doc.dialogue.md
